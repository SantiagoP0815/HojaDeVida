<style>
    body {
        /* Fondo fijo que cubre todo */
        background: url('/img/paramoaire.jpg') no-repeat center center fixed !important;
        background-size: cover !important;
        /* Evitamos doble scrollbar */
        overflow: hidden;
    }

    /* En móvil reactivamos el scroll del body */
    @media (max-width: 768px) {
        body {
            overflow: auto;
        }
    }

    /* Limpiar contenedores padres */
    .container-main,
    main,
    .main-layout {
        background: transparent !important;
        box-shadow: none !important;
        padding: 0 !important;
        margin: 0 !important;
        height: 100%;
    }
</style>

<div class="editor-layout">

    <aside class="editor-sidebar">
        <h5 class="mb-4 text-secondary ps-2 d-none d-md-block fw-bold" style="font-size: 0.9rem; letter-spacing: 1px;">
            NAVEGACIÓN</h5>

        <button type="button" class="menu-btn active" onclick="mostrarSeccion('datos', this)">
            <i class="fas fa-id-card"></i>
            <span>Datos Personales</span>
        </button>
        <button type="button" class="menu-btn" onclick="mostrarSeccion('formacion', this)">
            <i class="fas fa-graduation-cap"></i>
            <span>Formación</span>
        </button>
        <button type="button" class="menu-btn" onclick="mostrarSeccion('experiencia', this)">
            <i class="fas fa-briefcase"></i>
            <span>Experiencia</span>
        </button>
        <button type="button" class="menu-btn" onclick="mostrarSeccion('tiempo', this)">
            <i class="fas fa-clock"></i>
            <span>Tiempo Total</span>
        </button>
        <button type="button" class="menu-btn" onclick="mostrarSeccion('documentos', this)">
            <i class="fas fa-folder-open"></i>
            <span>Soportes</span>
        </button>

        <div class="sidebar-actions mt-auto pt-4 border-top d-none d-md-block">
            <button type="button" class="btn btn-success w-100 mb-2 shadow-sm" onclick="enviarFormulario()">
                <i class="fas fa-save me-2"></i> GUARDAR
            </button>
            <button type="button" class="btn btn-primary w-100 shadow-sm" onclick="descargarPDF()">
                <i class="fas fa-file-pdf me-2"></i> PDF
            </button>
        </div>
    </aside>

    <div class="editor-content">
        <form id="hojaForm" action="/hoja/insertar-datos" method="POST" enctype="multipart/form-data">
            <input type="hidden" name="_csrf" value="{{csrfToken}}">

            <div id="sec-datos" class="seccion-tab active">
                <div class="form-card">
                    <h2 class="section-title">1. DATOS PERSONALES</h2>
                    <div class="row g-3">
                        <div class="col-md-6"><label class="form-label">Primer Apellido</label><input type="text"
                                class="form-control" name="primer_apellido" value="{{user.primer_apellido}}" required>
                        </div>
                        <div class="col-md-6"><label class="form-label">Segundo Apellido</label><input type="text"
                                class="form-control" name="segundo_apellido" value="{{user.segundo_apellido}}"></div>
                        <div class="col-12"><label class="form-label">Nombres</label><input type="text"
                                class="form-control" name="nombres" value="{{user.nombres}}" required></div>
                    </div>
                    <hr class="my-4">
                    <h6 class="fw-bold text-muted mb-3">DOCUMENTO DE IDENTIFICACIÓN</h6>
                    <div class="row g-3">
                        <div class="col-md-3">
                            <label class="form-label">Tipo</label>
                            <select class="form-select" name="tipo_documento" required>
                                <option value="C.C" {{#if (eq user.tipo_documento "C.C" )}}selected{{/if}}>C.C</option>
                                <option value="C.E" {{#if (eq user.tipo_documento "C.E" )}}selected{{/if}}>C.E</option>
                                <option value="PAS" {{#if (eq user.tipo_documento "PAS" )}}selected{{/if}}>PAS</option>
                            </select>
                        </div>

                        <div class="col-md-4">
                            <label class="form-label">Número</label>
                            <input type="text" class="form-control" name="numero_documento"
                                value="{{user.numero_documento}}" required>
                        </div>

                        <div class="col-md-5">
                            <label class="form-label">Sexo</label>
                            <div class="d-flex gap-2 mt-1">
                                <input type="radio" class="btn-check" name="sexo" id="sexoM" value="M" {{#if (eq
                                    user.sexo "M" )}}checked{{/if}}>
                                <label class="btn btn-outline-primary flex-fill" for="sexoM"><i class="fas fa-male"></i>
                                    Masculino</label>

                                <input type="radio" class="btn-check" name="sexo" id="sexoF" value="F" {{#if (eq
                                    user.sexo "F" )}}checked{{/if}}>
                                <label class="btn btn-outline-primary flex-fill" for="sexoF"><i
                                        class="fas fa-female"></i> Femenino</label>
                            </div>
                        </div>
                    </div>

                    <div class="row g-3 mt-2">
                        <div class="col-md-6">
                            <label class="form-label">Nacionalidad</label>
                            <div class="d-flex gap-2 mt-1">
                                <input type="radio" class="btn-check" name="nacionalidad" id="nacCol" value="COLOMBIANO"
                                    {{#if (eq user.nacionalidad "COLOMBIANO" )}}checked{{/if}}
                                    onchange="togglePais(this)">
                                <label class="btn btn-outline-secondary flex-fill" for="nacCol">Colombiano</label>

                                <input type="radio" class="btn-check" name="nacionalidad" id="nacExt" value="EXTRANJERO"
                                    {{#if (eq user.nacionalidad "EXTRANJERO" )}}checked{{/if}}
                                    onchange="togglePais(this)">
                                <label class="btn btn-outline-secondary flex-fill" for="nacExt">Extranjero</label>
                            </div>
                        </div>

                        <div class="col-md-6">
                            <label class="form-label">País</label>
                            <input type="text" class="form-control" id="inputPais" name="pais" value="{{user.pais}}">
                        </div>
                    </div>
                    <hr class="my-4">
                    <h6 class="fw-bold text-muted mb-3">LIBRETA MILITAR</h6>
                    <div class="row g-3">
                        <div class="col-md-4">
                            <label class="form-label">Clase</label>
                            <select class="form-select" name="tipo_libreta_militar">
                                <option value="">Seleccione...</option>
                                <option value="PRIMERA CLASE" {{#if (eq user.tipo_libreta_militar "PRIMERA CLASE"
                                    )}}selected{{/if}}>Primera Clase</option>
                                <option value="SEGUNDA CLASE" {{#if (eq user.tipo_libreta_militar "SEGUNDA CLASE"
                                    )}}selected{{/if}}>Segunda Clase</option>
                            </select>
                        </div>
                        <div class="col-md-4"><label class="form-label">Número</label><input type="text"
                                class="form-control" name="numero_libreta_militar"
                                value="{{user.numero_libreta_militar}}"></div>
                        <div class="col-md-4"><label class="form-label">D.M</label><input type="text"
                                class="form-control" name="dm_libreta_militar" value="{{user.dm_libreta_militar}}">
                        </div>
                    </div>
                    <hr class="my-4">
                    <div class="row g-3">
                        <div class="col-12">
                            <h6 class="fw-bold text-muted">FECHA Y LUGAR DE NACIMIENTO</h6>
                        </div>
                        <div class="col-md-3"><label class="form-label">Fecha</label><input type="date"
                                class="form-control" name="fecha_nacimiento" value="{{user.fecha_nacimiento}}"></div>
                        <div class="col-md-3"><label class="form-label">País</label><input type="text"
                                class="form-control" name="pais_nacimiento" value="{{user.pais_nacimiento}}"></div>
                        <div class="col-md-3"><label class="form-label">Depto</label><input type="text"
                                class="form-control" name="departamento_nacimiento"
                                value="{{user.departamento_nacimiento}}"></div>
                        <div class="col-md-3"><label class="form-label">Municipio</label><input type="text"
                                class="form-control" name="municipio_nacimiento" value="{{user.municipio_nacimiento}}">
                        </div>
                    </div>
                    <div class="row g-3 mt-3">
                        <div class="col-12">
                            <h6 class="fw-bold text-muted">DIRECCIÓN DE CORRESPONDENCIA</h6>
                        </div>
                        <div class="col-12"><label class="form-label">Dirección</label><input type="text"
                                class="form-control" name="direccion_correspondencia"
                                value="{{user.direccion_correspondencia}}"></div>
                        <div class="col-md-4"><label class="form-label">País</label><input type="text"
                                class="form-control" name="pais_correspondencia" value="{{user.pais_correspondencia}}">
                        </div>
                        <div class="col-md-4"><label class="form-label">Depto</label><input type="text"
                                class="form-control" name="departamento_correspondencia"
                                value="{{user.departamento_correspondencia}}"></div>
                        <div class="col-md-4"><label class="form-label">Municipio</label><input type="text"
                                class="form-control" name="municipio_correspondencia"
                                value="{{user.municipio_correspondencia}}"></div>
                        <div class="col-md-6"><label class="form-label">Teléfono</label><input type="tel"
                                class="form-control" name="telefono" value="{{user.telefono}}"></div>
                        <div class="col-md-6"><label class="form-label">Email</label><input type="email"
                                class="form-control" name="email" value="{{user.email}}"></div>
                    </div>
                </div>
                <div class="text-end mb-5">
                    <button type="button" class="btn btn-primary shadow-sm"
                        onclick="mostrarSeccion('formacion')">Siguiente <i class="fas fa-arrow-right"></i></button>
                </div>
            </div>

            <div id="sec-formacion" class="seccion-tab">
                <div class="form-card">
                    <h2 class="section-title">2. FORMACIÓN ACADÉMICA</h2>
                    <div class="p-3 mb-4 rounded border bg-white">
                        <h6 class="fw-bold">EDUCACIÓN BÁSICA Y MEDIA</h6>
                        <p class="small text-muted mb-2">Marque el último grado aprobado:</p>
                        <div class="d-flex flex-wrap gap-2 mb-3 justify-content-center justify-content-md-start">
                            {{#each (range 1 12)}}
                            <div class="flex-grow-0">
                                <input type="radio" class="btn-check" name="educacion_basica" id="grado_{{this}}"
                                    value="{{this}}" {{#if (eq ../educacion_basica_media.educacion_basica (add ""
                                    this))}}checked{{/if}}>
                                <label class="btn btn-outline-primary fw-bold" for="grado_{{this}}"
                                    style="width: 50px; height: 40px; display: flex; align-items: center; justify-content: center;">
                                    {{this}}°
                                </label>
                            </div>
                            {{/each}}
                        </div>
                        <div class="row g-3">
                            <div class="col-md-8"><label class="form-label">Título Obtenido</label><input type="text"
                                    class="form-control" name="titulo_obtenido"
                                    value="{{educacion_basica_media.titulo_obtenido}}"></div>
                            <div class="col-md-4"><label class="form-label">Fecha Grado</label><input type="month"
                                    class="form-control" name="fecha_grado"
                                    value="{{educacion_basica_media.fecha_grado}}"></div>
                        </div>
                    </div>
                    <h6 class="fw-bold mt-4 mb-3">EDUCACIÓN SUPERIOR</h6>
                    <div id="container-estudios">
                        {{#if educacion_superior.length}}
                        {{#each educacion_superior}}
                        {{> estudioItem index=@index data=this}}
                        {{/each}}
                        {{else}}
                        {{> estudioItem index=0}}
                        {{/if}}
                    </div>
                    <button type="button" class="btn btn-outline-success btn-sm mt-2"
                        onclick="agregarBloque('container-estudios', 'estudio-item')"><i class="fas fa-plus"></i>
                        Agregar Estudio</button>
                    <hr class="my-4">
                    <h6 class="fw-bold mb-3">IDIOMAS</h6>
                    <div id="container-idiomas">
                        {{#if idiomas.length}}
                        {{#each idiomas}}
                        {{> idiomaItem index=@index data=this}}
                        {{/each}}
                        {{else}}
                        {{> idiomaItem index=0}}
                        {{/if}}
                    </div>
                    <button type="button" class="btn btn-outline-success btn-sm mt-2"
                        onclick="agregarBloque('container-idiomas', 'idioma-item')"><i class="fas fa-plus"></i> Agregar
                        Idioma</button>
                </div>
            </div>

            <div id="sec-experiencia" class="seccion-tab">
                <div class="form-card">
                    <h2 class="section-title">3. EXPERIENCIA LABORAL</h2>
                    <h6 class="text-primary fw-bold mt-2">EMPLEO ACTUAL</h6>
                    <div class="border border-primary border-opacity-50 rounded p-3 mb-4 bg-white">
                        <input type="hidden" name="id_experiencia_0" value="{{empleoActual.id_experiencia}}">
                        <input type="hidden" name="empleo_actual_0" value="SI">
                        <div class="row g-3">
                            <div class="col-md-8"><label class="form-label">Empresa</label><input type="text"
                                    class="form-control" name="empresa_entidad_0"
                                    value="{{empleoActual.empresa_entidad}}"></div>
                            <div class="col-md-4">
                                <label class="form-label">Tipo de Entidad</label>
                                <div class="d-flex gap-2 mt-1">
                                    <input type="radio" class="btn-check" name="tipo_0" id="tipo_pub_0" value="PUBLICA"
                                        {{#if (eq empleoActual.tipo "PUBLICA" )}}checked{{/if}}>
                                    <label class="btn btn-outline-success btn-sm flex-fill"
                                        for="tipo_pub_0">Pública</label>

                                    <input type="radio" class="btn-check" name="tipo_0" id="tipo_priv_0" value="PRIVADA"
                                        {{#if (eq empleoActual.tipo "PRIVADA" )}}checked{{/if}}>
                                    <label class="btn btn-outline-secondary btn-sm flex-fill"
                                        for="tipo_priv_0">Privada</label>
                                </div>
                            </div>
                            <div class="col-md-4"><label class="form-label">País</label><input type="text"
                                    class="form-control" name="pais_0" value="{{empleoActual.pais}}"></div>
                            <div class="col-md-4"><label class="form-label">Depto</label><input type="text"
                                    class="form-control" name="departamento_0" value="{{empleoActual.departamento}}">
                            </div>
                            <div class="col-md-4"><label class="form-label">Municipio</label><input type="text"
                                    class="form-control" name="municipio_0" value="{{empleoActual.municipio}}"></div>
                            <div class="col-md-6"><label class="form-label">Correo</label><input type="email"
                                    class="form-control" name="correo_entidad_0"
                                    value="{{empleoActual.correo_entidad}}"></div>
                            <div class="col-md-6"><label class="form-label">Teléfonos</label><input type="text"
                                    class="form-control" name="telefono_entidad_0"
                                    value="{{empleoActual.telefono_entidad}}"></div>
                            <div class="col-md-6"><label class="form-label">Ingreso</label><input type="date"
                                    class="form-control" name="fecha_ingreso_0" value="{{empleoActual.fecha_ingreso}}">
                            </div>
                            <div class="col-md-6"><label class="form-label">Retiro</label><input type="date"
                                    class="form-control" name="fecha_retiro_0" value="{{empleoActual.fecha_retiro}}">
                            </div>
                            <div class="col-md-6"><label class="form-label">Cargo</label><input type="text"
                                    class="form-control" name="cargo_actual_0" value="{{empleoActual.cargo_actual}}">
                            </div>
                            <div class="col-md-6"><label class="form-label">Dependencia</label><input type="text"
                                    class="form-control" name="dependencia_0" value="{{empleoActual.dependencia}}">
                            </div>
                            <div class="col-12"><label class="form-label">Dirección</label><input type="text"
                                    class="form-control" name="direccion_0" value="{{empleoActual.direccion}}"></div>
                            <div class="col-12"><label class="form-label text-primary">Soporte</label><input type="file"
                                    class="form-control" name="documento_experiencia_0"></div>
                        </div>
                    </div>
                    <h6 class="fw-bold text-muted">EMPLEOS ANTERIORES</h6>
                    <div id="container-empleos">
                        {{#if empleosAnteriores.length}}
                        {{#each empleosAnteriores}}
                        {{> empleoItem index=(add @index 1) data=this}}
                        {{/each}}
                        {{else}}
                        {{> empleoItem index=1}}
                        {{/if}}
                    </div>
                    <button type="button" class="btn btn-outline-success btn-sm mt-2"
                        onclick="agregarBloque('container-empleos', 'empleo-item')"><i class="fas fa-plus"></i> Agregar
                        Empleo</button>
                </div>
            </div>

            <div id="sec-tiempo" class="seccion-tab">
                <div class="form-card">
                    <h2 class="section-title">4. TIEMPO TOTAL</h2>
                    <div class="table-responsive">
                        <table class="table table-bordered bg-white">
                            <thead class="table-light">
                                <tr>
                                    <th>OCUPACIÓN</th>
                                    <th style="width:100px">AÑOS</th>
                                    <th style="width:100px">MESES</th>
                                </tr>
                            </thead>
                            <tbody>
                                <tr>
                                    <td>SERVIDOR PÚBLICO</td>
                                    <td><input type="number" class="form-control" name="anios_servidor_publico"
                                            value="{{tiempo_experiencia.anios_servidor_publico}}"></td>
                                    <td><input type="number" class="form-control" name="meses_servidor_publico"
                                            value="{{tiempo_experiencia.meses_servidor_publico}}"></td>
                                </tr>
                                <tr>
                                    <td>SECTOR PRIVADO</td>
                                    <td><input type="number" class="form-control" name="anios_sector_privado"
                                            value="{{tiempo_experiencia.anios_sector_privado}}"></td>
                                    <td><input type="number" class="form-control" name="meses_sector_privado"
                                            value="{{tiempo_experiencia.meses_sector_privado}}"></td>
                                </tr>
                                <tr>
                                    <td>INDEPENDIENTE</td>
                                    <td><input type="number" class="form-control" name="anios_trabajador_independiente"
                                            value="{{tiempo_experiencia.anios_trabajador_independiente}}"></td>
                                    <td><input type="number" class="form-control" name="meses_trabajador_independiente"
                                            value="{{tiempo_experiencia.meses_trabajador_independiente}}"></td>
                                </tr>
                                <tr class="fw-bold table-secondary">
                                    <td>TOTAL</td>
                                    <td><input type="number" class="form-control" name="anios_total_experiencia"
                                            value="{{tiempo_experiencia.anios_total_experiencia}}" readonly></td>
                                    <td><input type="number" class="form-control" name="meses_total_experiencia"
                                            value="{{tiempo_experiencia.meses_total_experiencia}}" readonly></td>
                                </tr>
                            </tbody>
                        </table>
                    </div>
                </div>
            </div>

            <div id="sec-documentos" class="seccion-tab">
                <div class="form-card">
                    <h2 class="section-title">SOPORTES</h2>
                    <div class="mb-4">
                        <label class="form-label">Documento de Identidad (Si falta)</label>
                        <input type="file" name="documento_id" class="form-control">
                    </div>
                    <h6 class="fw-bold">Archivos Cargados</h6>
                    <ul class="list-group mt-2">
                        {{#if documentos}}
                        {{#each documentos}}
                        <li class="list-group-item d-flex justify-content-between align-items-center bg-light">
                            <span>{{this.original_name}}</span>
                            <a href="/hoja/documento/{{this.id_documento}}" target="_blank"
                                class="btn btn-sm btn-outline-primary"><i class="fas fa-download"></i></a>
                        </li>
                        {{/each}}
                        {{else}}
                        <li class="list-group-item text-muted">No hay documentos adicionales.</li>
                        {{/if}}
                    </ul>
                </div>
            </div>

            <div class="d-md-none mobile-fab">
                <button type="button" class="btn btn-success shadow rounded-pill" onclick="enviarFormulario()"><i
                        class="fas fa-save"></i> Guardar</button>
                <button type="button" class="btn btn-primary shadow rounded-pill" onclick="descargarPDF()"><i
                        class="fas fa-file-pdf"></i> PDF</button>
            </div>
        </form>
    </div>
</div>

<script>
    function mostrarSeccion(id, btn) {
        document.querySelectorAll('.seccion-tab').forEach(s => s.classList.remove('active'));
        document.querySelectorAll('.menu-btn').forEach(b => b.classList.remove('active'));
        document.getElementById('sec-' + id).classList.add('active');
        if (btn) btn.classList.add('active');
        else {
            const b = document.querySelector(`button[onclick*="'${id}'"]`);
            if (b) b.classList.add('active');
        }
    }

    function togglePais(radio) {
        const input = document.getElementById('inputPais');
        if (radio.value.includes('COLOMBIA')) { input.value = 'COLOMBIA'; input.readOnly = true; }
        else { input.value = ''; input.readOnly = false; }
    }

    function enviarFormulario() { document.getElementById('hojaForm').submit(); }
    function descargarPDF() { window.location.href = '/hoja/generar-pdf'; }

    function agregarBloque(containerId, itemClass) {
        const container = document.getElementById(containerId);
        const items = container.querySelectorAll('.' + itemClass);

        if (items.length >= 5) return alert('Máximo 5 registros.');

        // Clonar el primer elemento
        const clone = items[0].cloneNode(true);

        // Calcular nuevo índice
        let newIndex = items.length;
        if (containerId === 'container-empleos') newIndex = items.length + 1;

        // Limpiar y actualizar IDs y Nombres
        clone.querySelectorAll('input, select, label').forEach(el => {
            // Limpiar valor
            if (el.tagName === 'INPUT' || el.tagName === 'SELECT') {
                el.value = '';
                if (el.type === 'radio' || el.type === 'checkbox') el.checked = false;
            }

            // Actualizar NAME (ej: empresa_0 -> empresa_1)
            if (el.name) el.name = el.name.replace(/_\d+$/, '_' + newIndex);

            // Actualizar ID (ej: tipo_pub_0 -> tipo_pub_1)
            if (el.id) el.id = el.id.replace(/_\d+$/, '_' + newIndex);

            // Actualizar FOR (para los labels)
            if (el.getAttribute('for')) {
                const oldFor = el.getAttribute('for');
                el.setAttribute('for', oldFor.replace(/_\d+$/, '_' + newIndex));
            }
        });

        // Eliminar enlaces/botones basura si se clonaron
        const links = clone.querySelectorAll('a');
        links.forEach(l => l.remove());

        container.appendChild(clone);
    }
</script>

{{#*inline "estudioItem"}}
<div class="estudio-item dynamic-item">
    <input type="hidden" name="id_educacion_superior_{{index}}" value="{{data.id_educacion_superior}}">
    <div class="row g-3">
        <div class="col-md-6"><label class="form-label">Modalidad</label><select class="form-select"
                name="modalidad_academica_{{index}}">
                <option value="">Seleccione...</option>
                <option value="TC" {{#if (eq data.modalidad_academica "TC" )}}selected{{/if}}>TC</option>
                <option value="TL" {{#if (eq data.modalidad_academica "TL" )}}selected{{/if}}>TL</option>
                <option value="UN" {{#if (eq data.modalidad_academica "UN" )}}selected{{/if}}>UN</option>
                <option value="ES" {{#if (eq data.modalidad_academica "ES" )}}selected{{/if}}>ES</option>
                <option value="MG" {{#if (eq data.modalidad_academica "MG" )}}selected{{/if}}>MG</option>
            </select></div>
        <div class="col-md-6"><label class="form-label">Título</label><input type="text" class="form-control"
                name="nombre_titulo_{{index}}" value="{{data.nombre_titulo}}"></div>
        <div class="col-md-3"><label class="form-label">Semestres</label><input type="number" class="form-control"
                name="semestres_aprobados_{{index}}" value="{{data.semestres_aprobados}}"></div>
        <div class="col-md-3"><label class="form-label">Graduado</label><select class="form-select"
                name="graduado_{{index}}">
                <option value="SI" {{#if (eq data.graduado "SI" )}}selected{{/if}}>SI</option>
                <option value="NO" {{#if (eq data.graduado "NO" )}}selected{{/if}}>NO</option>
            </select></div>
        <div class="col-md-3"><label class="form-label">Terminación</label><input type="month" class="form-control"
                name="mes_terminacion_{{index}}" value="{{data.mes_terminacion}}"></div>
        <div class="col-md-3"><label class="form-label">Tarjeta</label><input type="text" class="form-control"
                name="numero_tarjeta_profesional_{{index}}" value="{{data.numero_tarjeta_profesional}}"></div>
        <div class="col-12"><label class="form-label">Soporte</label><input type="file" class="form-control"
                name="documento_educacion_superior_{{index}}"></div>
    </div>
</div>
{{/inline}}

{{#*inline "idiomaItem"}}
<div class="idioma-item dynamic-item">
    <input type="hidden" name="id_idioma_{{index}}" value="{{data.id_idioma}}">
    <div class="row g-3">
        <div class="col-12"><label class="form-label">Idioma</label><input type="text" class="form-control"
                name="idioma_{{index}}" value="{{data.idioma}}"></div>
        <div class="col-4"><label class="form-label">Habla</label><select class="form-select" name="habla_{{index}}">
                <option>R</option>
                <option>B</option>
                <option>MB</option>
            </select></div>
        <div class="col-4"><label class="form-label">Lee</label><select class="form-select" name="lee_{{index}}">
                <option>R</option>
                <option>B</option>
                <option>MB</option>
            </select></div>
        <div class="col-4"><label class="form-label">Escribe</label><select class="form-select"
                name="escribe_{{index}}">
                <option>R</option>
                <option>B</option>
                <option>MB</option>
            </select></div>
    </div>
</div>
{{/inline}}

{{#*inline "empleoItem"}}
<div class="empleo-item dynamic-item">
    <input type="hidden" name="empleo_actual_{{index}}" value="NO">
    <input type="hidden" name="id_experiencia_{{index}}" value="{{data.id_experiencia}}">
    <div class="row g-3">
        <div class="col-md-8"><label class="form-label">Empresa</label><input type="text" class="form-control"
                name="empresa_entidad_{{index}}" value="{{data.empresa_entidad}}"></div>
        <div class="col-md-4">
            <label class="form-label">Tipo</label>
            <div class="d-flex gap-2 mt-1">
                <input type="radio" class="btn-check" name="tipo_{{index}}" id="tipo_pub_{{index}}" value="PUBLICA"
                    {{#if (eq data.tipo "PUBLICA" )}}checked{{/if}}>
                <label class="btn btn-outline-success btn-sm flex-fill" for="tipo_pub_{{index}}">Pública</label>

                <input type="radio" class="btn-check" name="tipo_{{index}}" id="tipo_priv_{{index}}" value="PRIVADA"
                    {{#if (eq data.tipo "PRIVADA" )}}checked{{/if}}>
                <label class="btn btn-outline-secondary btn-sm flex-fill" for="tipo_priv_{{index}}">Privada</label>
            </div>
        </div>
        <div class="col-md-4"><label class="form-label">País</label><input type="text" class="form-control"
                name="pais_{{index}}" value="{{data.pais}}"></div>
        <div class="col-md-4"><label class="form-label">Depto</label><input type="text" class="form-control"
                name="departamento_{{index}}" value="{{data.departamento}}"></div>
        <div class="col-md-4"><label class="form-label">Municipio</label><input type="text" class="form-control"
                name="municipio_{{index}}" value="{{data.municipio}}"></div>
        <div class="col-md-6"><label class="form-label">Email</label><input type="email" class="form-control"
                name="correo_entidad_{{index}}" value="{{data.correo_entidad}}"></div>
        <div class="col-md-6"><label class="form-label">Teléfono</label><input type="text" class="form-control"
                name="telefono_entidad_{{index}}" value="{{data.telefono_entidad}}"></div>
        <div class="col-md-6"><label class="form-label">Ingreso</label><input type="date" class="form-control"
                name="fecha_ingreso_{{index}}" value="{{data.fecha_ingreso}}"></div>
        <div class="col-md-6"><label class="form-label">Retiro</label><input type="date" class="form-control"
                name="fecha_retiro_{{index}}" value="{{data.fecha_retiro}}"></div>
        <div class="col-md-6"><label class="form-label">Cargo</label><input type="text" class="form-control"
                name="cargo_actual_{{index}}" value="{{data.cargo_actual}}"></div>
        <div class="col-md-6"><label class="form-label">Dependencia</label><input type="text" class="form-control"
                name="dependencia_{{index}}" value="{{data.dependencia}}"></div>
        <div class="col-12"><label class="form-label">Dirección</label><input type="text" class="form-control"
                name="direccion_{{index}}" value="{{data.direccion}}"></div>
        <div class="col-12"><label class="form-label">Soporte</label><input type="file" class="form-control"
                name="documento_experiencia_{{index}}"></div>
    </div>
</div>
{{/inline}}